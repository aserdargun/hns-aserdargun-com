import { SourceLink } from '../../components/SourceLink'
import { catalog } from '../../content/catalog'
import type { Locale } from '../../content/schema'
import { getLocalizedText } from '../../content/selectors'

export function PatternsPage({ locale }: { locale: Locale }) {
  return <section className="library-page"><header className="library-header"><p className="section-kicker">{locale === 'tr' ? 'Tekrarlanabilir sistem kararları' : 'Repeatable system decisions'}</p><h1>{locale === 'tr' ? 'Harness desenleri' : 'Harness patterns'}</h1><p>{locale === 'tr' ? 'Her desen mekanizma, bedel, kanıt izi ve ölçülebilir deney fikriyle birlikte sunulur.' : 'Each pattern includes its mechanism, tradeoffs, evidence trail, and a measurable experiment idea.'}</p></header><div className="pattern-list">{catalog.patterns.map((pattern, index) => <article id={pattern.id} key={pattern.id} className="pattern-entry"><div className="pattern-title"><span>0{index + 1}</span><h2>{getLocalizedText(pattern.title, locale)}</h2><p>{getLocalizedText(pattern.summary, locale)}</p></div><div className="pattern-anatomy"><section><h3>{locale === 'tr' ? 'Mekanizma' : 'Mechanism'}</h3><p>{getLocalizedText(pattern.mechanism, locale)}</p></section><section><h3>{locale === 'tr' ? 'Bedeller' : 'Tradeoffs'}</h3><p>{getLocalizedText(pattern.tradeoffs, locale)}</p></section><section><h3>{locale === 'tr' ? 'Deney fikri' : 'Experiment idea'}</h3><p>{getLocalizedText(pattern.experiment, locale)}</p></section><section className="pattern-sources"><h3>{locale === 'tr' ? 'Kanıt izi' : 'Evidence trail'}</h3>{pattern.sourceIds.map((id) => { const source = catalog.sourcesById.get(id); return source ? <SourceLink key={id} source={source} locale={locale} /> : null })}</section></div></article>)}</div></section>
}
